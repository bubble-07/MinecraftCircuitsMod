N-Bit D-Latch

An N-Bit D-latch has a configurable bitwidth n.
There is 1 n-bit input, one 1-bit control input, and 1 n-bit output.

If the 1-bit input is low, the circuit will set its output to match the input.
If the 1-bit input is high, the circuit will retain its current output value.